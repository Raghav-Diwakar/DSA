#include <stdio.h>
#include <stdlib.h>
#define size 10
int isempty();
void push();
int pop();
void DecToBin();
int main()
{
    int num;
    printf("enter  the number to convert \n");
    scanf("%d", &num);
    printf("binary number is \n");
    DecToBin(num);
    return 0;
}
void DecToBin (int num ){
    iff 
}